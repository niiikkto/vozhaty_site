services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - SECRET_KEY=django-insecure-docker-compose-key-for-dev
    volumes:
      - .:/app
      - sqlite_data:/app/
    command: python manage.py runserver 0.0.0.0:8000

volumes:
  sqlite_data: